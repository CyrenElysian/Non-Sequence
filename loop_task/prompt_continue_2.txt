The scenario is "{scenario}".
One of the steps towards that is:{steps}.
Step {loop_idx} "{loop_step}" involves a loop.
Because {{please_insert}}, {reason_one}.

(Give a more underlying reason/phenomenon than the one above. The new reason must be different and less immediately visible, but still directly implies the loop should continue.)
Only output the text that should replace {{please_insert}}.